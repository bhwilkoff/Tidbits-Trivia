# Tidbits Trivia — Cross-Platform Feature Parity

> **Single source of truth** for what's shipping where. Updated in
> the SAME change set as any user-facing feature.
>
> Companion to `CLAUDE.md` (project context), `SCRATCHPAD.md` (active
> milestone), `DECISIONS.md` (architecture decisions). Per-platform
> **binding** design rules live in `docs/iOS-DESIGN.md`,
> `docs/tvOS-DESIGN.md`, `docs/WEB-DESIGN.md`, `docs/ANDROID-DESIGN.md`,
> and `docs/macOS-DESIGN.md` (authored 2026-07-03). The full workflow —
> including the periodic parity audit — is the
> `cross-platform-parity-discipline` skill.
>
> **Android TV / Fire TV (7th platform) — 2026-08-29:** the SAME Android app
> now serves TVs. `leanback` and `touchscreen` are declared `required="false"`
> so one Play listing covers phones, tablets and TVs; a `LEANBACK_LAUNCHER`
> category makes it visible in the TV launcher (without it the app installs and
> is invisible), and `res/drawable-xhdpi/tv_banner.png` is the 320x180 banner.
> Every Android feature is therefore present by construction — the parity
> question on TV is not *does the feature exist* but **can a remote reach it**,
> which is a different axis from the table below.
>
> That axis is measured, not asserted: `tools/tv_focus_audit.py` walks all 26
> surfaces, confirms each shows its OWN content (by OCR signature) and that
> something holds focus which OK can act on. **26/26 on both the Fire TV and
> the Google TV dongle.** Re-run it after any UI change.
>
> TV-specific deltas from the phone build, all `isTv()`-gated so the phone is
> untouched: a visible focus ring on every card and answer (Compose focuses
> happily and draws nothing); the first answer option claims focus per question;
> "Press OK" replaces "Tap"; overscan padding; and **free recall uses the tvOS
> recall-then-reveal self-mark** instead of a text field, because typing on a
> remote is a keyboard wall — the same call the Apple TV build made.
>
> NOT done: **Fire TV ships through the Amazon Appstore, not Google Play** —
> a separate console and submission. The Play **Android TV form-factor opt-in
> is Console-only** (the androidpublisher API has no form-factor resource) and
> triggers a separate Google TV review, so it is an OWNER step; see
> `docs/ANDROID-TV-PLAY-SETUP.md`.
>
> **macOS (5th platform) — 2026-07-03:** the native Mac app is BUILT and
> launches (Decision 042/043). Full parity face — Play, Records (R-REC-1
> dashboard), Create, Settings + ⌘,/⌘N menu commands, Customize (mix +
> presets), Daily archive, onboarding, solo Trivia Night, Versus CPU +
> Online-Multiplayer picker, deep links, Game Center dashboard — all native,
> reusing Core verbatim, build- + screenshot-verified. Plus the Mac-exclusive
> **Tidbits Live** pub-event system (event builder, host cockpit with
> reveal-on-command + manual score override, tie-break engine, big-screen
> projector window, offline + printable answer sheets, venue branding).
> **Networked Tidbits Live team-join now BUILT on every platform** (2026-07-03):
> a Mac host publishes to Firebase RTDB `live/{code}` and **web / iOS / iPadOS /
> tvOS / Android** players join by code (unified "Join a game" front — probes RTDB
> for a hosted event, else falls back to the LAN Trivia Night). Build- +
> render-verified; the live multi-device run is the owner gate (deploy the `live`
> rules + a real Mac host). DEFERRED still (⏳, need a serverless transport +
> 2-device testing): networked Trivia-Night host/join, online Quick Match, and the
> Tidbits Live dependent formats (fastest-finger, audio, poll, cheating
> deterrence). See `docs/macOS-DESIGN.md` §A7 + `docs/EVENT-TRIVIA-COMPETITIVE.md`.
>
> **Windows (6th platform) — 2026-07-18: SHIPPED to the Microsoft Store (in
> certification). A fully supported channel (Decision 046).** Native Avalonia 12 /
> FluentAvalonia 3 / .NET 10 app at **~full parity with the Mac app** — every
> consumer, host (Tidbits Live Waves A–E), social, portable-identity,
> live-generation, online-multiplayer and duels feature built in a C# port of the
> shared logic (`Tidbits.Core`), byte-compatible on the shared RTDB
> `live/{code}` + `queue/mixed` plane so a Windows player matches a phone/web
> player. **222 tests green on `windows-latest`.** The Windows per-item matrix is
> `docs/WINDOWS-PARITY.md` (kept as the authoritative Windows column so this
> file's 4-platform tables stay readable); the top-line consumer + host verbs are
> all met.
>
> **Store submission is DONE — first submission is in certification** (Store ID
> `9NRKS9LDRCWC`, MSIX `LearningisChangeInc.TidbitsTrivia` v1.6.45). The one-time
> Partner Center bootstrap (Entra app + Manager role, 4 secrets, name reservation,
> first manual submission incl. age ratings) is complete; every future ship is
> `gh workflow run windows-store.yml -f submit=true -f commit=true` — see
> `docs/WINDOWS-STORE-SUBMISSION.md`. Two non-obvious Store blockers were solved
> and recorded there: the required `runFullTrust` justification (Submission
> Options), and the "access policies document" banner cleared by the Xbox-services
> **Test** button (product type = Game).
>
> The old "needs a real Windows box" blocker class is **retired** (Decision 045):
> there is no free Windows VM, so `windows-latest` IS the Windows machine, CLI-driven
> via `windows-repl.yml` with a headless-PNG harness for observability. That
> unblocked **0.2 Win32 interop** (taskbar progress + global hotkeys) and **1.22
> DPAPI** (which also fixed a real exposure: the Firebase refresh token was written
> to disk in cleartext).
>
> Genuinely open (none block parity or the Store): **3.34 video picture** [~] — the
> LibVLC-frames→WriteableBitmap pipeline is built + verified with synthetic frames,
> but LibVLC invoking the callbacks on a real video file needs a Windows CI fixture;
> a few [~] polish/depth items in `docs/WINDOWS-PARITY.md` (Live builder pointer
> drag-reorder, extend-titlebar/theme-follow). Stack + rationale:
> `docs/WINDOWS-DESIGN.md`, `docs/WINDOWS-PLAYBOOK.md`, `docs/WINDOWS-RESEARCH.md`,
> `docs/WINDOWS-STORE-SUBMISSION.md`.
>
> **Last audit: 2026-08-24 (tvOS, DEVICE-verified on real hardware).** The
> external-observation campaign (docs/TVOS-TEST-PLAYBOOK.md) device-verified
> 40+ tvOS rows — all 14 modes, 4 Versus bots, picture rounds, cross-platform
> RTDB night join, live Create, all Club surfaces, Release config — and found
> the unwired-debug-hook class (F-001..F-003, fixed + re-verified). tvOS cells
> claiming ✅ in this file now have glass evidence behind them.
>
> **Prior audit: 2026-06-30 (Android ↔ Apple, code-verified).** Found 4
> false cells (§3b emoji grid, §4 entire auth section, §5 share URLs,
> §12 adaptive icon) and confirmed the real Android-behind-iOS gaps
> (onboarding, full Settings + attribution, haptics, pass-and-play).
> Core gameplay is at genuine parity. Corrections applied below.

---

## Legend

- ✅ **Shipped** — live in production on this platform
- 🚧 **In progress** — being built; some parts may already be in main
- ⏳ **Planned** — committed; targeted for an upcoming milestone
- 🔮 **Future** — agreed direction; no timeline yet
- 🚫 **Out of scope** — explicitly not built on this platform (with reason)
- n/a — platform-inapplicable (e.g., lock-screen controls on tvOS)

A ⏳ or 🚫 cell carries its reason in Notes. "Deliberately deferred,
because X" is a healthy cell; a silent blank is drift.

---

## Parity rule

When shipping any user-facing feature:

1. **Confirm the verb is identical across platforms.**
   Find = explore, Profile = identify, etc. Don't let one platform
   own a different verb for the same surface.
2. **Pick the native idiom per platform** — `<dialog showModal>` on
   web, `.sheet` on iOS, focus-driven full-screen on tvOS,
   `ModalBottomSheet` on Android.
3. **Update this table** in the SAME PR. Drift here is what causes
   "the web has X but iOS doesn't" complaints six months later.
4. **Cross-link to the binding design doc** for each platform that
   has one.

---

## 0. Platform set

Web + iOS + iPadOS + tvOS + Android (Decision 020), **macOS the fifth**
(Decision 042), **Windows 10/11 the sixth** (Decision 046) — **six shipping
platforms, one feature set.** tvOS earns its place because living-room trivia
is lean-back (Decision 021); macOS + Windows earn theirs because trivia is
genuinely lean-in (macOS rides the universal Apple target nearly free; Windows
rides Avalonia + a $0 CI-only pipeline, Decision 045). iOS is the lead
platform; everything ships there first, then mirrors. A platform not yet
reached is ⏳ with a note, never silence.

**How the two newest platforms are tracked in this matrix:** the tables below
carry the four original columns (Web / iOS / tvOS / Android) to stay readable.
**macOS** rides the shared Apple `Core/` and is BUILT (see the macOS prose
block above + `docs/macOS-DESIGN.md`). **Windows** is BUILT and SHIPPED and has
its own authoritative per-item column in **`docs/WINDOWS-PARITY.md`** — treat
that file as the Windows cell for every row here. When you ship a feature, update
BOTH this matrix (the 4 columns + Notes) AND the relevant Windows/macOS tracker,
same change set — that's what keeps "fully supported channel" honest.

## 1. Top-level navigation

| Verb | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
| Play vs CPU (online-multiplayer v0, Decision 038) | ✅ | ✅ | ✅ | ✅ | **2026-07-02:** the home "Online Multiplayer" surface is live — one sheet/panel (Quick Match marked coming-soon = the v1 slot) + four CPU opponents (The House adapts to the player's recent accuracy; Rookie/Regular/Ace). Believable-bot spec mirrored 3× (`BotOpponent.swift`/`Bots.kt`/`js/bots.js`): category-varying skill, log-normal timing, ~5% freeze; same Scoring as the player. **Every bot is visibly labeled CPU.** Live strip + per-reveal outcome + final standings; iOS + Android live-verified, tvOS build-verified (GC interstitial blocks the headless sim), web syntax-checked. Matches don't write records (same as pass-and-play) |
| Online Quick Match (real players) | ✅ | 🚧 | ✅ | 🚧 | **Two native paths (owner-approved):** Apple = **GameKit** (Decision 039; `GKMatchmakerViewController` → `GameKitTransport` behind the seam → LiveNight, auto-paced) — iOS/tvOS BUILT, 2-device Game Center hardware test is the gate. **Android + web = Firebase RTDB** (Decision 040) since Google killed PGS multiplayer: anon-auth-gated rooms, transaction-claimed quick-match queue, leader publishes a shared canonical-`WireQuestion` set, self-scored, live standings. **Android hardware-verified** (emulator: anon auth + room + live 2-player roster sync + shared-set match launch); **web room lifecycle REST-verified 11/11**. Cross-platform (web↔Android) uses the identical wire shape. The 2-device cross-platform test is the final gate |
| Play (home) — Quick Play + progressive disclosure | ✅ | ✅ | ✅ | ✅ | **Redesigned 2026-07-01 (rule R-HOME-1, Decision 035), revised same day (Decision 036 / R-HOME-1a):** ONE single-action Quick Play hero; **Surprise + Customize are a quiet secondary pair under it** (no button-in-button); prominent Daily; unified Trivia Night; **home tile = Online Multiplayer placeholder** (Create lives in its tab). Native idiom per platform (iOS `.sheet`, Android `ModalBottomSheet`, web `<dialog>`, tvOS focus picker + chip row). **R-ICON-1: platform icon systems only (SF Symbols / Material Symbols / inline SVG) — no emoji chrome** |
| Create — varied retrieval (diversity caps) + shape blend + saved sets | ✅ | ✅ | ✅ | ✅ | **2026-07-03 (owner):** Create dropped the "which continent is X" template + trivially-easy tier, and **caps questions per category** (round-robin) so a sports/geography-dense topic can't monopolize the 8 (all 3 engines: `search`/`Corpus.search` + `diversify`). Sets now **blend shapes** — corpus MCQ + topic-matched Picture ID / This-or-That / Closest Call — played as a shuffled `mix`. **Saved sets** (replay; future: share) shipped on web (localStorage list in Create); iOS/Android ⏳ save-UI is the fast follow | · **2026-07-31 (W4):** the shareable saved-quiz mechanic now has a frozen wire contract (`docs/QUIZ-CONTRACT.md`, `quiz.v1`) implemented on Apple/Android/Windows/web with a shared golden fixture; web's older `tidbits.savedSets` is superseded and needs the §7 migration. Local persistence + UI + sharing are the next waves · **2026-07-31 shelf:** iOS + web + Android now create-and-keep automatically, replay from refs, and web/iOS can share (`#/quiz/<id>` + `tidbitstrivia://quiz/<id>`). shelves shipped on ALL SIX platforms 2026-07-31 (Windows also migrates its own pre-contract saved-sets store). tvOS gets Create too — chips over typing, focus over buttons, and QR-code sharing since a TV has no clipboard or share sheet (QR verified scannable by decoding it back out of a screenshot). Quizzes sync across a player's devices via playersPrivate/{uid}/quizzes. **Multiple game types shipped on all six surfaces 2026-07-31** (the `m` field was being recorded and ignored — a quiz saved as Survival replayed as mix). **Android + Windows share/deep-link shipped 2026-07-31** (verified end to end: a quiz created on an Apple TV opened on Android from its share link) · **2026-08-01 topic-drift fix, all four engines:** play-testing the 984 most-viewed Wikipedia articles through the shipped assembly on the iOS simulator measured **49.8% of every question as off-topic** — "Ansel Adams" returned Hansel and Gretel, "Harry Kane" returned Spokane, "Denver" returned Bob Denver, and "Zlatan Ibrahimović" returned a picture of Neymar (the topic was matching as a DISTRACTOR). Relevance is now a **floor, not a ranking** (`tier`): word-bounded matching, agentive-tag-only admission, a different-person guard, and diversification only WITHIN a tier. **0.6% drift** on the top 300. Mirrored in Swift / Kotlin / C# / JS with the same 9-case regression suite on each (plus `tools/create/web-mirror-check.mjs`); `tools/create/playtest.py --sweep` scores a real device log. **Sweep extended to all 984 topics 2026-08-01**, which surfaced a class the first 300 never contained: a regnal numeral or an initial is dropped as "too short", so **"George VI" returned George Martin, George Mallory, George Eliot and Paul George**, and "O. J. Simpson" returned Homer, Bart and Marge. When the phrase still holds a non-stopword the token list threw away, only the phrase can be trusted (`phraseIsRequired`) — and it deliberately does NOT fire for "The Beatles", where the dropped word is a stopword. **Corpus repair 2026-08-01** (`tools/corpus/fix_prompt_disambiguators.py`): 8,141 templated prompts printed the Wikipedia disambiguator at the player — "Who wrote Austerlitz (novel)?", "On which continent is Iskar (river)?" — and 32 of them printed the ANSWER ("Who composed Magnificat (Bach)?"). The same class `fix_match_giveaways.py` fixed for Match-Up, never swept across the MCQ families. Stripped, leaks dropped, 122 refused as unsafe; resynced to all platforms with the Daily golden re-verified. Also: a topic that is nothing but stopwords ("From (TV series)") no longer matches every row containing that word. **2026-08-01 (2nd pass):** the repair was over-stripping — removing "(Taylor Swift song)" left "In what year was Cruel Summer first released?", ambiguous where the original was merely ugly. Only pure TYPE descriptors are stripped now ("(film)", "(river)", "(1927 song)"); one that NAMES something is left alone. Plus three more drift fixes: subject identity ignores a disambiguator (the corpus titles the rapper "Drake (musician)", so nothing was titled plainly "Drake", the person-guard never armed, and typing "Drake" returned Nick Drake and Drake & Josh); a prompt occurrence inside a DIFFERENT proper name no longer counts ("Denver" was matching "...and John Denver", "Michael Jackson" a Glenda Jackson biopic) while a capitalised predecessor the player TYPED still does ("John Lennon and Paul McCartney"); and the shape sources (picture / this-or-that / closest) now arm the same person-guard, which is how a closest-call round was still asking what year John Denver died. **2026-08-01 (3rd pass):** an agentive tag alone no longer admits a row at all — it was the last measurable source of drift ("Rod Stewart" returned Britt Ekland's height off a "Partners of" tag; "Vajiralongkorn" returned seven questions about his wives and daughters, none naming him). A tag still contributes to scoring. Full 984-topic simulator sweep: **49.8% → 0.9% drift**. **2026-08-01 live-question grammar:** reading the live-generated quizzes for the 377 uncovered topics off the simulator found a systematic fragment — "Which British politician who has served as Chancellor of the Exchequer under Andy Burnham since 20 July 2026?" — because the "Which X?" stem only works on a bare noun phrase. A clue carrying a finite relative clause now takes "Name this ..." instead, in all four engines. **2026-08-01 cross-stack parity now MEASURED, not asserted** (`tools/create/parity.sh`): the four engines were unit-tested against the same cases, but nothing compared what they actually SELECT for a real topic over the real 128k corpus. It found a divergence on its first run — Swift's sort is not stable and JavaScript's is, so rows tied on score ordered differently and `diversify`'s per-category cap then kept a different SET. Ties now break on id in all four. **49 topics, 0 differences** between the iOS simulator and the real js/api.js. The Apple selection is now a committed golden (`tools/create/golden/search.txt`) that C# asserts against too (`CreateGoldenTest`), so three of the four engines are compared on real output rather than on shared unit cases — **19 of the 49 topics correctly return NOTHING and are in the golden on purpose**, since a golden listing only the topics with results would pass a regression that brought Spokane and Butane back for "Harry Kane". **All four engines now assert against that golden** — Kotlin's ranking policy was split out of `search` into a `Corpus.rank` seam that runs on the JVM over every corpus row, closing the last unverified engine
| Trivia Night — real rounds system + clear setup | ✅ | ✅ | ✅ | ✅ | **2026-07-03 (owner):** setup preset cards now list the FULL round lineup (name + question count per round, no abbreviations, no horizontal rails — wrapped grids per Decision 036); solo/pass nights show a **round interstitial** ("ROUND 1 OF 5 · General Knowledge · 6 questions · Start Round") via a new engine `roundIntro` phase, so rounds are FELT not just banner-swapped. Networked nights stay host-paced (no interstitial). Verified: Android live (setup lineup + interstitial), iOS/tvOS/web build+boot |
| Custom Mix — multi-select modes in Customize | ✅ | ✅ | 🚫 | ✅ | **2026-07-03 (owner request):** mode chips multi-select; 2+ modes launch a `mix` meta-mode drawing across ALL selected modes, shuffled (reuses the night's shape-driven engine — no rounds/banners). Remembered by Quick Play (`lastMixModes`); presets store `modeIDs`. tvOS 🚫 = the ten-foot picker is selection-is-action (Decision 036 inversion) — multi-select contradicts it; revisit if owners ask |
| Quick Play + saved presets (power-user) | ✅ | ✅ | ✅ | ✅ | Last-(mode,category) resolves the Quick Play default; saveable presets; Surprise = random. Customize sheet self-explains modes (selected mode's blurb line); sentence-case labels everywhere. tvOS: category-select starts the game (documented ten-foot inversion, Decision 036) and has no presets (⏳ low-value on TV) |
| Records (stats, streak, review) | ✅ | ✅ | ✅ | ✅ | **Redesigned 2026-07-01 (Task 8):** removed the confusing domains Pie; per-domain rows explain the level ("N more to Level X"); spelled-out labels ("Accuracy"/"Correct", "Level"). Achievement taxonomy authored in `docs/achievements.json` (GC + Play creation via API is owner-blocked). Android via SharedPreferences |
| Create (quiz from any topic) | ✅ | ✅ | 🚫 | ✅ | **Corpus-grounded 2026-07-01 (Task 7):** retrieves REAL vetted corpus questions matching the topic (no hallucination), falls back to live generation only when thin. tvOS: typing on a Siri Remote is hostile — n/a |
| Cross-context day streak (visible) | ✅ | ✅ | ✅ | ✅ | **L2, 2026-07-04:** the identity's forgiving cross-context streak (any solo game OR live night keeps it; freeze covers a gap) surfaced at the habit moments — results streak card (count, "best ever", freezes banked), daily banner/card, and the share. Records shows this ONE streak of record (legacy per-device `DailyStreak` retained only for the Game Center leaderboard). Encouragement, never punishing (no countdowns) |
| Spoiler-free shareable result | ✅ | ✅ | ✅ | ✅ | Wordle-grid emoji share (🟢🔴⚫️) + accuracy meter + day streak + play link; never reveals answers (recipient still plays). Web Share API / native ShareLink / Android chooser. Existed since Decision 041; L2 added the day streak |
| Daily log — cross-device sync (signed in) | ✅ | ✅ | ✅ | ✅ | **L2, 2026-07-04:** when signed in, daily completions ride `dailyLog/{emailKey}` on the $0 plane, so "done today" is respected on every device and the archive is the union of all plays (anon plays push up on sign-in; anonymous stays local-only). macOS via the shared Core |

---

## 2. Core gameplay (single-player)

| Feature | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
| **Trivia Night** (configurable "bar trivia" night) | ✅ | ✅ | ✅ | ✅ | The new flagship default mode, on **all four platforms**: a host-configured night of themed **rounds**, each round drawing one question TYPE — so one night pulls from EVERY question type (general MCQ → picture → which-first → closest → ordering → matching → name-it → odd-one-out → name-as-many). Presets (Quick / Pub / The Works) + category (iOS also per-round counts). A client meta-mode over the shape-routing engine (no corpus change — Decisions 025/031). Round banners + end-of-round beats + the shared missed-fact recap. **iOS** home card → `NightSetupView` → `NightContainerView`. **tvOS** home hero → ten-foot `NightSetupView_tvOS` → `TVNightContainer`. **web** coral banner → native `<dialog>` setup → the shared game loop, plus shareable `#/night`·`#/night/quick`·`#/night/works` deep links. **Android** coral home card → `NightSetupScreen` → `Route.Game(BAR_TRIVIA, nightRounds=…)`. Solo everywhere; **every platform (incl. web) now hosts AND joins a networked night** over the shared RTDB backend (Decision 044 — see §3b "Trivia Night — networked") |
| Classic mode (10 Qs, speed scoring) | ✅ | ✅ | ✅ | ✅ | Same loop on every platform (Swift/JS/Kotlin mirrors) |
| Time Attack (60s) | ✅ | ✅ | ✅ | ✅ | |
| Survival (until one wrong) | ✅ | ✅ | ✅ | ✅ | |
| Stake (confidence allocation) | ✅ | ✅ | ✅ | ✅ | 8-Q round; fixed chip budget (Sure×2/Likely×3/Hunch×3), commit before answering; adds-only, never negative — calibration, not gambling (Decision 022). Solo "home version" of LearnedLeague/Pour House (GAME-MODES-RESEARCH M1) |
| Sweep (fill-the-set, beat-your-best) | ✅ | ✅ | ✅ | ✅ | 12-Q "set"; +1 per correct (count-scored, no speed bonus) with a persistent fill-grid scoreboard (mint hit / coral miss); beat your own best via existing per-mode best-score. Sporcle "sweep the set" home version (SOLO-BACKLOG M2) |
| Picture ID (identify the image) | ✅ | ✅ | ✅ | ✅ | 10-Q mode off the E1 enrichment (`picture.json`, 4,813 Qs): a Commons image + a **type-aware, varied stem** + the corpus's 4 vetted options. `gen_picture.py:picture_stem()` classifies each subject from its one-line description and asks the RIGHT question — a person gets "Who is this Finnish ski jumper?", a place "What city is this?", an event "Name this siege." — rotating across ~4 phrasings (1,954 distinct stems; bare "What is this?" down from 100% to 10%, kept only for genuinely typeless subjects). Stem lives in the data, so all four platforms inherit it with no client change. Occupation now wins over embedded work/place nouns ("video game designer" → a person, not "What video game is this?"), and both explanation formats (describe + cloze) are parsed. Two corpus-wide cleanups ride along (`recategorize_and_clean.py`, all modes benefit): **category repair** of 740 miscategorized people (athletes filed under Film & TV → Sports, etc., only when the description doesn't support the current category) and **option-disambiguator stripping** of 2,718 rows (a distractor like "John Thomson (photographer)" beside bare names was a giveaway; collision cases like "Russo-Ukrainian war (2022–present)" keep their qualifier). iOS/tvOS AsyncImage, web `<img>`, Android Coil — each with a load-failure fallback (this mode needs the network; all other modes stay offline). SOLO-BACKLOG Q7 |
| Which First? / This-or-That (chronology) | ✅ | ✅ | ✅ | ✅ | 10-Q binary pick off E1 chronology (`thisorthat.json`, 779 Qs): "Which came first?" + "Which has the bigger population/area?" between two same-category entities (verified 0 chronology errors). Renders on the existing 2-option MCQ surface. SOLO-BACKLOG Q1 |
| Closest Call (numeric estimation) | ✅ | ✅ | ✅ | ✅ | 8-Q proximity mode off E1 numbers (`closest.json`, 1,233 Qs: year / atomic number / elevation). Estimate on a slider (iOS/web/Android); tvOS has no Slider so it uses ±coarse/±fine focusable steppers. Adds-only proximity scoring; the first non-MCQ type — earns custom numeric data (Decision 031). SOLO-BACKLOG M5 |
| In Order / Ordering (chronology) | ✅ | ✅ | ✅ | ✅ | 6-Q "arrange earliest→latest" off E1 years (`order.json`, 394 Qs, 4 items each). Uniform ↑/↓ move buttons on every platform (incl. tvOS — no drag). Partial credit by inversion count, adds-only (Decision 031). SOLO-BACKLOG Q4. **2026-08-01 scoring fix (all 4 stacks): partial credit is now measured above CHANCE.** The board is dealt shuffled, and a shuffled 4-item list already has ~half its pairs in the right relative order — so `40 * (1 - inversions/max)` started every round at half marks. Playing all 126 mode×category combinations to lose (`TIDBITS_PLAYTHROUGH_STYLE=timeout`) measured a mean of **119/240 for never touching the board** (floor 87), more than most modes pay for playing well. Now chance = 0 and above-chance rescales to the full 40; a perfect arrangement still pays 240/240. Still adds-only (Decision 022). 6 new tests in `OrderScoringTests.swift` |
| Match Up / Matching (pairs) | ✅ | ✅ | ✅ | ✅ | 6-Q "link each key to its value" from the corpus's 1:1 Wikidata relations (`match.json`, 136 Qs: country→capital/currency, element→symbol, book→author). Tap key then value to link; partial credit by correct links, adds-only. tvOS = focusable key rows + value chips. SOLO-BACKLOG Q5. **2026-08-01 content pass** (found by playing Match Up on the simulator — "Cædmon's Hymn → Cædmon"): 36 pairs across 8% of rounds were solvable by reading alone (San Marino → San Marino, Singapore → Singapore dollar, Russia → Russian ruble, Real Madrid → Madrid) — now filtered by whole-word AND 4+ char stem overlap. Also: pairs ranked by row difficulty (a straight regeneration had produced "Hoysala Kingdom → Halebidu"); prompts corrected to place/work/title ("Match each **country** to its capital" was asked of Idaho and Cornwall); generic money values dropped (gold/coin/bimetallism can't identify one key in a 1:1 grid). **Regenerating deleted all 22 hand-authored sports/history rounds** — they lived only in the generated artifact; now committed under `tools/corpus/authored/` and merged by id, along with the 67 authored Odd One Out rows and 28 Enumerate puzzles. 340 rounds, 0 read-off pairs. Data file, so all 6 platforms take it. **2026-08-01: 340 -> 1,145 rounds, every category 48-224 (was 8-13 in sports/business/history).** Two fixes: four relations the corpus already held were never used (`rel:P17` country, `P112` founded-by, `P175` performer, `P170` creator), and the round builder took four CONSECUTIVE pairs and discarded the group when two shared a value — fine for capital-of, useless for country-of where dozens share an answer, which is why adding the relations produced ZERO rounds until the builder became a round-robin over value buckets. Cap is now per (relation, category); one shared budget had let whichever category sorted first eat all 60. **Match Up now holds 100/100/100% on-category over three consecutive plays** (was 100/90/65%) |
| Name It / Type-the-answer (free recall) | ✅ | ✅ | ✅ fallback | ✅ | 8-Q free-text recall (`typeanswer.json`, 997 Qs) matched against an accepted set (answer + E1 aliases, diacritic/punct/case/"the"-insensitive). iOS/web/Android = text field; **tvOS** = recall-then-reveal self-mark (text entry is a keyboard wall at ten feet) — graceful per-platform idiom. SOLO-BACKLOG Q6 |
| Odd One Out (which doesn't belong) | ✅ | ✅ | ✅ | ✅ | 8-Q "which doesn't belong?" built from the corpus's country→continent data (`oddoneout.json`, 67 Qs: 3 from one continent + 1 outlier). Plain 4-option MCQ (outlier = answer) — reuses the existing answer surface, zero new UI. SOLO-BACKLOG Q3 |
| Ladder (climb easy→hard) | ✅ | ✅ | ✅ | ✅ | 10-Q MCQ round sorted by the F3 derived difficulty (`difficulty.json` overlay: Wikipedia pageviews → 1..5; popular=easy); harder rungs pay a climb bonus. Client-mode wrapper over the corpus, zero new UI. SOLO-BACKLOG F3. **2026-08-01 fix (all 4 stacks): Ladder ignored the picked category** — it pulled the pool from `"mixed"` regardless, so a Geography ladder was 13% geography (chance). Found by playing all 126 mode×category combinations on the simulator (`TIDBITS_PLAYTHROUGH`). Fixed in Swift/JS/Kotlin/C# in one change set |
| **Weak-Spot Arena** 🔒 Club | ✅ | ✅ | ✅ | ✅ | **First Tidbits Club EXCLUSIVE feature (Decision 047, `docs/CLUB-FEATURES-BUILD.md` Feature 1). Shipped all 6 platforms** — web ✅ · iOS ✅ · macOS ✅ · tvOS ✅ · Android ✅ (1.6.48) · Windows ✅ (1.6.48, 300 tests green; windows-latest CI green). A Club-gated mode that builds a 10-Q round entirely from YOUR unresolved misses (most-missed + oldest-first), tops up from your weakest domains when misses are thin (floor 4 / fill 8 / cap 10), shows a plain "why you're seeing this" reason per question ("Missed 2 weeks ago · ×3" / "Shoring up {category}"), and pays off with a **"You closed N gaps"** tally (excludes fill questions). The legitimate deeper layer above the free spaced-review weave (row §3b). **Non-members see the mode card + a real preview → the existing paywall, never a blank wall (no upsell).** Excluded from Customize/Surprise-Me/remembered defaults on every platform. Debug override: `TIDBITS_CLUB=1` / `?club=1` / BuildConfig flag. Same verb, native idiom |
| Category coverage disclosure (a mode can't fill a category) | ✅ | ✅ | ✅ | ✅ | **2026-08-01.** The picker offered 9 mode×category combinations the bundle cannot fill — **Business has zero rows in every bundled shape source**, so Business + Picture ID served a round with no business questions and said nothing (measured over 1,260 assembled rounds). Categories a mode can't fill are now **dimmed with a line saying what you'll actually get** — dimmed, not disabled: the round still plays, and removing the choice is worse than telling the truth. Logic (`QuestionProvider.coverage`/`canFill`, JS `canFillCat`) is in the shared layer on Apple + web; **2026-08-03: all four remaining pickers wired**, each in its own idiom — tvOS dims the category CARD and swaps its blurb for what you'll actually get (never disabled: on a TV a disabled card is a focus dead end), macOS dims the chip in the Customize sheet plus a caption, Android dims the `FilterChip` plus a caption, and Windows — which picks the mode by BUTTON, so there is no per-chip dimming to do — shows one line naming the modes the chosen category cannot fill. The logic moved into each stack's shared layer (`Coverage` in Kotlin, `QuestionSources.CanFill` in C#, 3 new Windows tests pinning the arithmetic), not into the views. **Verification is uneven and worth stating: tvOS/macOS/iOS builds green + 510 Windows tests green + Windows headless build; the ANDROID chip is compile-verified only** — the emulator on this dev box wedged repeatedly (`hanging thread 'QEMU2 CPU1'`) after the earlier Android work was verified on it, so the dimming has not been seen on a screen. Real fix behind it is content: generate business rows for the shape sources. **2026-08-01 update: 9 -> 3 unfillable combinations.** `gen_business_shapes.py` derives Business Picture ID (+76), Which First? (+37) and In Order (+18) from Commons images and founding years; `tools/corpus/authored/business.py` adds Match Up (8), Odd One Out (10) and Name as Many (4) by hand. Both APPEND — never regenerate — so no existing id is touched. Remaining: typeAnswer x business, enumerate x science/music. **Known and NOT yet fixed: thin categories deplete.** Odd One Out carries ~11 rows per category against an 8-question round, so a player's SECOND round in that category falls back to mixed — measured by running the sweep twice on one device, oddOneOut goes 100% -> 31.2% on-category. 17 of 64 combinations hold less than two rounds' worth. The picker's `canFill` still uses one round as its threshold, so it stays silent about this; the real fix is content, not a threshold. **2026-08-01 (owner: "there are 128K questions, there should be no thin categories") — right, and the corpus was never the thin part.** The SHAPE SOURCES were: `oddoneout.json` 238 rows and `enumerate.json` 32, because their generators used exactly one grouping (country→continent) while the corpus carries a dozen 1:1 relations. `gen_oddoneout_relations.py` now derives from all of them (director/composer/author/continent/capital/currency/P17/P170/P175/P37/P112/P61): **oddoneout 238 → 930 rows, 11-12 per category → 23-321**. `gen_enumerate_relations.py` adds 139 creator puzzles (32 → 171). Measured by playing the whole grid THREE times on one device, Odd One Out holds **100% / 100% / 98%** on-category (was 100% → 31% on the second pass). Match Up still decays 100/90/65% — it has 8-13 rows in sports/business/history and needs the same treatment. **2026-08-01 FINAL: 0 of 64 mode x category combinations short at one round; 1 at two rounds (typeAnswer x business, 14/16).** Whole 126-combination grid played THREE times on one device: **100.0% / 99.9% / 99.4%** on-category overall, every mode 95-100% flat. Was 49.8% topic drift and oddOneOut collapsing to 31% on a second play at the start of the day. The picker's dimming/caption now effectively never fires, which is the right end state — it exists for content gaps, and there are none. **Era spread — FIXED, and this cell said otherwise for two days.** It read "Open, measured, NOT fixed: 445 of 4,858 MCQs whose four options are all dated people (9.2%) span more than 400 years". `fix_era_distractors.py` closed it the same day (commit 32d537d, "distractors now share the answer's walk of life") — 401 repaired by occupation+era, 44 unrepairable and dropped — and it is now a **gate rule with a budget of zero**: `ERA-SPREAD` fails the build on a single reappearance, and reads 0 today. A finding that has become a gate rule does not need a parity note; it needs the gate. Corrected 2026-08-03. |
| Name as Many / enumeration (Q8) | ✅ | ✅ | ✅ fallback | ✅ | 3-puzzle "name as many X in 60s" round (`enumerate.json`, 11 puzzles / 243 slots: continent→countries + curated planets/elements/oceans/Great Lakes/continents). Type against a live clock; each unique answer (alias-matched via the type-answer normalizer + E1 aliases) fills a chip; +1 each (count-scored). Reveal shows the full set, named vs. missed (testing effect). iOS/web/Android = text field; **tvOS** = recall-self-mark (reveal list + count stepper — keyboard wall at ten feet). Replayable drill: ignores the seen-set. Data curated (historical/defunct states + dupes dropped, PRC→China). SOLO-BACKLOG Q8 |
| Daily Tidbit (deterministic, streak) | ✅ | ✅ | ✅ | ✅ | **2026-07-01 audit: the old "same seed on all platforms" note was silently false** — three different seed strings/hashes/pools/shuffles meant every platform picked a DIFFERENT daily (owner caught it). Now: one canonical hash-rank pick (Decision 037, `DailyPick.swift`/`pickDailyIds`/`pickDaily`), golden-proven identical on all three stacks via `tools/daily-parity/run.sh` (which also flushed out Kotlin's signed-byte FNV divergence on non-ASCII ids) |
| Interactive Records — game history, drill-ins, richer share | ✅ | ✅ | ✅ | ✅ | **2026-07-03 (owner):** per-game answer detail now persisted (`GameRecord.answers` / Rec.answers / localStorage) so Records is interactive. **"Your games" history** (Threes-style scroll) on all 4 — each game a card with a category-colored answer-dot strip + score + relative time, tap → per-question recap (question · answer · right/wrong). **Domain drill-in** (Missed / Got right) + **personal-best drill-in** (every attempt, best crowned) via sheets on iOS/Android/web; tvOS has the history scroll + recap (drill-in sheets ⏳ = touch idiom). **Share redesigned**: answer dots (🟢/🔴/⚫️) + accuracy meter (▰▱) + 🔥 best-run, replacing uniform squares. Old records degrade gracefully (totals only). Verified live on iOS + Android |
| Records-as-dashboard (R-REC-1: preview + "See all") | 🚧 | ✅ | ✅ | 🚧 | **2026-07-03 (owner: "kitchen sink / squished"):** Records was dumping up to 40 game cards inline plus every domain/mode — the overload + claustrophobia bug. New binding rule **R-REC-1** (iOS-DESIGN §5.3–5.6 / WEB-DESIGN §5 / ANDROID-DESIGN §5): Records is a *dashboard* — show the **3 most recent** games + a **"See all N games"** drill-in sheet, never the full ledger inline. **iOS** (`RecordsView.swift`, build-verified ✅) + **web** (`app.js`/`styles.css`, syntax-verified; browser screenshot ⏳ extension offline) done this pass; web also fixed the flush-stacked `.game-row` shadow-collision (the "squished" half). **Android** ⏳ mirror (`RecordsScreen` still `records.take(40)`). **tvOS** already summarized (bounded at 20; tvOS-DESIGN §7). Follow-up ⏳: fold the chunky-card shadow gutter into the `chunkyCard` modifier app-wide (iOS-DESIGN §7.1) — a 10-file layout refactor needing a sim screenshot pass |
| Daily play-once + Previous Tidbits archive (R-DAILY-1) | ✅ | ✅ | ✅ | ✅ | **2026-07-01 (Decision 036):** today's Daily locks after completion (card flips to done-state with score; no Play Again on results); archive lists last 30 days — unplayed past days playable via the deterministic day-key seed; past plays never bump the streak. Per-day results: UserDefaults (Apple) / SharedPreferences (Android) / localStorage (web) |
| 9 categories | ✅ | ✅ | ✅ | ✅ | Mixed/History/Science/Geography/Arts/Film&TV/Music/Sports/Business |
| Countdown clock + speed bonus | ✅ | ✅ | ✅ | ✅ | Per-question or global per mode |
| Streak multiplier | ✅ | ✅ | ✅ | ✅ | Capped at 2× (bounded reward) |
| "Learn the fact" reveal + Wikipedia link | ✅ | ✅ | ✅ | ✅ | The mission-critical screen. **2026-07-01 audit: the Android cell was silently false** — its reveal lacked the correct/lightbulb status icon and the "Read <title> on Wikipedia" source link both iOS + web had. Fixed same day: Material `Verified`/`Lightbulb` badge, richer explanation type, `SportsScore` round-complete icon (was a 🏁 emoji), and the source link via `LocalUriHandler` — emulator-verified opening Chrome |
| Post-game missed-fact recap | ✅ | ✅ | ✅ | ✅ | Full "Tidbits to remember" list — every miss + answer + cited fact. tvOS in a focusable ScrollView; Android in the results scroll (SOLO-BACKLOG F2) |
| Four content states (load/empty/error/offline) | ✅ | ✅ | ✅ | ✅ | Web: service-worker offline; native: bundled corpus |
| Emoji-grid result on screen | ✅ | ✅ | ✅ | ✅ | + share intent (Android), ShareLink (iOS), Web Share |

---

## 3. Content pipeline (shared data plane — see docs/DATA-CONTRACT.md)

| Feature | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
| Bundled offline corpus (10k, never-repeat) | ✅ JSON→IndexedDB | ✅ SQLite | ✅ SQLite | ✅ JSON (in-memory) | One corpus, per-platform reader. Apple and Android read corpus.sqlite ONLY; corpus.json is a web/Windows asset and bundling it into an app is 54.9MB of dead weight (check_mirrors reports it as STRAY) |
| Live generation from any Wikipedia topic | ✅ | ✅ | ✅ (fallback) | ✅ | Powers Create + corpus fallback; web hits the API with origin=* (CORS). **2026-08-01:** measured on the simulator, this was returning ZERO questions for 28 of 30 uncovered topics — Apple Intelligence reported `available` and threw on every call (guardrail asset), the fame floor wanted a 600-char extract from an endpoint that returns a lead paragraph, distractors demanded an exact type sibling from a 4-article pool, and the lead-sentence parser could not survive an appositive. Fixed on the Apple side (9 → 34 questions on the 20 hardest topics), **mirrored to Android / web / Windows 2026-08-01** — all four now share the relaxed fame floor, the coarse-class distractor fallback, the separated distractor pool, the appositive-tolerant lead parser and the on-topic phrase filter |
| Template engine + quality gates | ✅ js/engine.js | ✅ Swift | ✅ (shared Core) | ✅ Kotlin | Four mirrors of `tools/corpus/generate_corpus.py` |
| Wikidata structured questions (the moat) | ✅ | ✅ | ✅ | ✅ | ~2,850 verified Qs in the shared corpus (Decision 024); gates 1/2/4/5 by construction |
| Question TYPE variety (29 types) | ✅ | ✅ | ✅ | ✅ | 5 summary shapes + 17 Wikidata + 6 fact types: forward/reverse attribute, superlative, chronology, numeric closest-to, classification, who-directed/wrote/composed, birth/death-year, nationality — all 4-option, rendered via the shared corpus (Decisions 025/027) |
| Deep article fact-extraction (fact:* types) | ✅ | ✅ | ✅ | ✅ | SHIPPED: 1,000 `fact:*` Qs in the shared corpus (6 types: directed/written/composed_by, birth/death_year, nationality) via `tools/corpus/wiki_extract.py` (Decision 027). Build-time only — all 4 read the corpus unchanged |
| Vandalism/NPOV gates 6/7 + human sampling 9 | 🔮 | 🔮 | 🔮 | 🔮 | Next corpus step (e.g. contested continent-of-country cases) |
| E1 Wikidata enrichment (image/numeric/alias) | 🚧 | 🚧 | 🚧 | 🚧 | DATA shipped: `assets/enrich.json` (1,591 entities) + `assets/picture.json` (816 Picture ID Qs) via `tools/corpus/enrich.py`+`gen_picture.py`. Additive, separate from corpus. Consumer modes (Picture ID, Closest Call, …) read it — UI per platform is the next wave (SOLO-BACKLOG E1) |

---

## 3b. Records, sharing, multiplayer

| Feature | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
| Personal bests + lifetime stats | ✅ | ✅ | ✅ | ✅ | SwiftData (Apple); localStorage (web); SharedPreferences (Android). tvOS shown in `RecordsView_tvOS` |
| Topic Levels (per-domain XP/level) | ✅ | ✅ | ✅ | ✅ | QuizUp's best idea: an XP level + bar per knowledge domain, derived from game history (no new persistence). Shared `ProgressMath` (Core) mirrored in store.js/Tidbits.kt and now rendered on tvOS (SOLO-BACKLOG M4) |
| The Pie (breadth wedges) | ✅ | ✅ | ✅ | ✅ | Trivial-Pursuit pie: a wedge per domain earned at a small mastery bar (≥15 correct, ≥60% acc); completes only when all 7 domains filled — fights corpus bias. Same shared derivation; tvOS Canvas pie in `RecordsView_tvOS` (SOLO-BACKLOG M3) |
| Stake calibration readout (F1) | ✅ | ✅ | ✅ | ✅ | Per-tier hit-rate (Sure/Likely/Hunch) in Records, accumulated across Stake rounds — the self-knowledge mirror. iOS persists via a `CalibrationTally` SwiftData model; web localStorage; Android SharedPreferences; tvOS reads the same model in `RecordsView_tvOS` |
| Answer-distribution telemetry (F4) | ✅ | ✅ | ✅ | ✅ | Local-first **foundation**: privacy-respecting per-option answer counts keyed by question id (`tidbits.answerTelemetry`), written on every game-end via `RecordsStore.recordTelemetry` (iOS/tvOS, UserDefaults) / `Store.recordTelemetry` (web localStorage, Android SharedPreferences). No PII, no network; synthetic-chosenIndex modes (closest/ordering/matching/type) skipped. Invisible infra — the **Predict the Crowd** "X% picked this" reveal stays 🔮 until a backend aggregates these across players (deferred to Ben). SOLO-BACKLOG F4 |
| Daily streak + missed-fact review | ✅ | ✅ | ✅ | ✅ | Streak on all 4; spaced re-asking of missed questions now woven into games on all 4 (Android by question-id via `Corpus.byId`), each with an opt-out toggle |
| Compete vs. your past self | ✅ | ✅ | ✅ | ✅ | New-best detection on each game; tvOS surfaces personal bests in `RecordsView_tvOS` |
| **Story Archive** 🔒 Club | ✅ | ✅ | ✅ | ✅ | **2nd Tidbits Club EXCLUSIVE (Decision 047, `docs/CLUB-FEATURES-BUILD.md` Feature 2). Shipped all 6 platforms** — web ✅ · iOS ✅ · macOS ✅ · tvOS ✅ · Android ✅ (1.6.49) · Windows ✅ (1.6.49, 318 tests green; windows-latest CI green). Club keeps every "story behind the answer" (`Question.explanation`) you've unlocked, as a permanent **searchable/filterable library** (by text, domain, favorited ⭐, missed/mastered) off Records (R-REC-1 "see all"). Tap a story → full text + **"Re-ask this"** 1-Q drill. A per-qid `SeenStory` is upserted on EVERY answered question (right or wrong) in the shared record write-path, freezing the question snapshot so a story survives corpus edits. **CRITICAL — additive, not a lock (R-MON-1):** the in-moment story reveal stays FREE on every platform; Club only adds the persistent library surface (free users see each story once, in passing). **Non-members see a real preview → the paywall, never a blank wall.** tvOS drops text search for filter chips (ten-foot keyboard-wall rule). Debug: `TIDBITS_CLUB=1` / `?club=1`. Same verb, native idiom |
| **Knowledge Atlas** 🔒 Club | ✅ | ✅ | ✅ | ✅ | **4th Tidbits Club EXCLUSIVE (Decision 047, `docs/CLUB-FEATURES-BUILD.md` Feature 4). Shipped all 6 platforms** — web ✅ · iOS ✅ · macOS ✅ · tvOS ✅ · Android ✅ · Windows ✅ (1.6.51, tests green; windows-latest CI green). Your accuracy **by domain over a trailing 12 months** + a **decay radar** (domains strong ≥70% 6–11mo ago that have dropped ≥12pts), computed **purely** from the existing game-record history (no new store; same source as the free Topic Levels/Pie — which stay untouched, R-MON-1). **The anti-Sporcle guard: every number is a door** — each domain row launches a domain-scoped round (the same category-filtered launch Quick Play uses), decay rows carry a "Shore it up" round; it interprets AND acts, never a passive dashboard. Off Records (R-REC-1). Constants identical across platforms (sampleFloor 8 / strong 0.70 / decayDelta 0.12). Taxonomy is flat (9 categories) so sub-domain drill doesn't apply yet. **Non-members see a real strongest/weakest preview → the paywall, never a blank wall.** tvOS focus-driven; Debug: `TIDBITS_CLUB=1` + `TIDBITS_ATLAS=1` / `?club=1`. Same verb, native idiom |
| **Link Wall** 🔒 Club | ✅ | ✅ | ✅ | ✅ | **6th Tidbits Club EXCLUSIVE (Decision 047, `docs/CLUB-FEATURES-BUILD.md` Feature 6). Built all 6 platforms** — web ✅ · iOS ✅ · macOS ✅ · tvOS ✅ · Android ✅ · Windows ✅ (1.6.51, 24 LinkWall tests green; windows-latest CI green). A **NYT-Connections-style SECOND daily** — 16 fact-tiles, 4 hidden groups of 4; the **free Daily Tidbit is untouched**, Link Wall is the Club daily. A deterministic daily puzzle GENERATOR (`LinkWall`, one impl per platform, ported byte-for-byte — Swift/JS/Kotlin/C# agree on the same groups per date) over the corpus's structured `match.json` relation-sets (country→capital, element→symbol, composer→work, …), with the day picked via the shared `DailyPick` hash so everyone gets the same puzzle. **Quality-gated (memory `quality-over-volume-steer`):** a first sample was 4/8 broken (the capital/author/director/composer pools were built for the Matching mode, where impure decoys are harmless, but a labeled Connections group exposes them); a scoped data-cleanliness pass (sovereign-capital + real-book/film/classical allowlists, pool-and-recombine into synthetic clean 4-tile blocks, a near-duplicate guard, singleton-theme downweighting) took it to **8/8 content-clean** — all in `LinkWall.*`, `match.json` untouched (it still serves Matching). Play: 4×4 grid, select 4 → submit, 4 mistakes, "one away", correct groups collapse (yellow→green→blue→purple), loss reveal w/ cited why, win + a shareable colored-square grid; **one puzzle/day**, persisted (resume mid-progress + completed-day lock). tvOS separates focus-state (ring+scale) from selection-state (mint+checkmark). Non-members → the paywall w/ a real preview, never a blank wall. **KNOWN LIMITATION (owner decision, NOT a blocker):** thin clean pools repeat ~every 1–2 weeks and skew obscure (fair + teachable, but not hand-crafted-NYT delight) — the curated-vs-generated / corpus-enrichment choice is the owner's; the play surface + generator are reusable under any choice. Debug: `TIDBITS_CLUB=1` + `TIDBITS_LINKWALL=1` / `?club=1`. Same verb, native idiom |
| **Marathon** 🔒 Club | ✅ | ✅ | ✅ | ✅ | **3rd Tidbits Club EXCLUSIVE (Decision 047, `docs/CLUB-FEATURES-BUILD.md` Feature 3). Shipped all 6 platforms** — web ✅ · iOS ✅ · macOS ✅ · tvOS ✅ · Android ✅ · Windows ✅ (335 tests green; windows-latest CI green). Version 1.6.51. A 200-question graded endurance run whose load-bearing NEW mechanic is **resume across sessions**: a `MarathonRun` SwiftData model (fixed ordered 200-id list drawn once from a stored seed, `currentIndex`, per-answer JSON results) persists after EVERY answer, so a crash/quit never loses progress — hardware-verified via a real `simctl terminate` mid-run (SQLite inspection of `default.store` confirmed `currentIndex`/results survived intact, and resume continued into the SAME stored id list, not a regenerated one). On finish, writes a permanent `MarathonScore` (score = difficulty-weighted, correct/total, per-domain `MarathonDomainStat` breakdown, duration) and clears the run. iOS surface: a dedicated teal Club card (distinct from the quick modes) showing "Question N of 200 — Resume" with a Resume/Start-Over confirmation when a run is in progress; the scorecard shows per-domain accuracy bars + "+N% vs your last run" + a permanent Marathon History list off the card and off Records (R-REC-1 "see all"). **Non-members see a real preview (illustrative domain-scorecard sample, since there's no free-tier Marathon data to draw from) → the existing paywall, never a blank wall.** Excluded from Customize/Surprise-Me/remembered defaults. Debug: `TIDBITS_CLUB=1` + `TIDBITS_MARATHON_LEN=<n>` (shortens the run for testing; production is always 200) + `TIDBITS_MARATHON=1` (auto-launch/resume). **Android (1.6.50/vc72, mirror of the Apple reference):** `data/Marathon.kt` (`MarathonRun`/`MarathonScore`/`MarathonDomainStat` + the generator, using the same `stableSeed` rank-and-slice the Daily uses) persisted via new `Store` SharedPreferences methods (`marathonRun`/`saveMarathonRun`/`clearMarathonRun`/`marathonHistory`/`appendMarathonScore`); `Mode.MARATHON` excluded from `playableModes`/remembered/Surprise-Me; teal Home card (Resume chip + AlertDialog Resume/Start-Over) + Records "Marathon History" card, both mirroring the Weak-Spot/Story-Archive Club-card pattern; `GameScreen` persists every answer immediately via a `LaunchedEffect(game.answered.size)` and writes the scorecard the instant the run completes; `Mode.MARATHON` skipped entirely in `GameState.end()`'s record/miss/seen/identity writes. Debug: reused `tidbits_club_debug` + a new `marathon_len` intent extra (`Marathon.debugLengthOverride`, `BuildConfig.DEBUG`-gated). `assembleDebug` BUILD SUCCESSFUL; emulator-verified end to end including a REAL `am force-stop` mid-run (SharedPreferences inspection confirmed `currentIndex`/results survived and a cold relaunch resumed into the SAME stored id list), full completion → scorecard → history → run cleared, and a real bug caught + fixed during verification (see the build log entry). Same verb, native idiom |
| **Expedition** 🔒 Club | ✅ | ✅ | ✅ | ✅ | **5th Tidbits Club EXCLUSIVE (Decision 047, `docs/CLUB-FEATURES-BUILD.md` Feature 5). Shipped all 6 platforms** — web ✅ · iOS ✅ · macOS ✅ · tvOS ✅ · Android ✅ · Windows ✅ (1.6.51, 383 tests green; windows-latest CI green). web/macOS/tvOS/Windows all mirror the same portable Expedition catalog + progress/certificate persistence + hub/map surface (only stage Play is Club-gated; hub+map are a real preview reachable by all); Windows icons via FASymbolIcon (no emoji, R-ICON-1). A multi-week **structured campaign** through one domain — an ordered list of themed **stages**, a visible map/path, and a completion **certificate**. NOT a new game engine: `Expeditions.startStage` filters the bundled corpus by a stage's `categoryID` + a DIFFICULTY BAND (the taxonomy is FLAT, same constraint the Knowledge Atlas hit, so stages differentiate by difficulty rather than sub-domain) and routes into the existing `.classic` launch path with a pass bar (e.g. 6/10 to advance). 3 hand-defined 7-stage expeditions ship (The 20th Century / Around the World / The Scientific Record); the catalog (`Core/Models/ExpeditionModels.swift`) is additive — more expeditions need no client change. Persistence mirrors Marathon: `ExpeditionProgress` (unlike Marathon's at-most-one run, SEVERAL expeditions may be in progress at once, keyed by `expeditionID`) advances `currentStageIndex` on a pass and persists across sessions; the last stage passing writes a permanent `ExpeditionCertificate` and clears progress — both hardware-verified via `simctl terminate` + SQLite inspection of `default.store` (a stage pass survived a full relaunch; the certificate write deleted the progress row while a SECOND expedition's independent in-progress row was untouched). iOS surface: an Expeditions destination reachable by EVERYONE — the list and an expedition's stage map (locked/current/done markers) are a real preview; only the Play tap is Club-gated → the existing `ClubPaywallView`, never a blank wall. A pass shows a "Stage N passed" beat and unlocks the next; a real (non-forced) fail shows "Needed 6 of 10 — you got 3" and offers Try Again, verified with actual corpus questions. Excluded from Customize/Surprise-Me/remembered defaults. Debug: `TIDBITS_CLUB=1` + `TIDBITS_EXPEDITION=1` (open the list) + `TIDBITS_EXPEDITION_FORCE_PASS=1` (verification: a stage always records as a pass regardless of score) + `TIDBITS_EXPEDITION_MAP=<id>` / `TIDBITS_EXPEDITION_AUTOPLAY=<id>:<stageIndex>` (verification-only: jump straight to a map/stage — this dev box has no GUI Simulator window to tap through). **Android (mirror of the Apple reference):** `data/Expeditions.kt` (the SAME 3 seven-stage campaigns, `questionCount`=10/`passBar`=6) filters `Corpus.pull(categoryId, …)` by each stage's real `Question.difficulty` band (never-empty fallback to the whole category pool); persistence is a `Store.expeditionProgress()` SharedPreferences JSON object keyed by expeditionId (several campaigns in progress at once) + a separate `expeditionCertificates` array. A stage plays as an ordinary `Mode.CLASSIC` round with a `custom` question list — `GameState.end()` needed no special-casing, a stage genuinely writes a normal `GameRecord`; only `GameScreen`'s result branch is special-cased (`Route.Game.expeditionId`/`expeditionStageIndex` → `Expeditions.recordStageResult` → a dedicated `ExpeditionStageResultScreen` pass/fail/certificate beat instead of `ResultsScreen`). Mint Club `ExpeditionsCard` on Home always opens the hub (never the paywall directly) since the hub + map are curated content reachable by everyone; only the map's Play button is Club-gated → `ClubPaywallScreen`. Debug: reused `tidbits_club_debug` + a new `expedition_force_pass` intent extra (`Expeditions.debugForcePass`, `BuildConfig.DEBUG`-gated, mirrors Apple's force-pass hook). `assembleDebug` BUILD SUCCESSFUL; emulator-verified: a REAL (non-forced) round scored 1/10 → "NOT QUITE — Needed 6 of 10... you got 1" → Try Again → stage did NOT advance (SharedPreferences-confirmed); the force-pass hook drove a full 7-stage completion → certificate written + progress row deleted in the same edit; both an in-progress failed campaign and a completed certificate survived two cold `am force-stop` relaunches, staying independent of each other throughout; non-member hub/map rendered as a full preview and Play routed to the paywall. Same verb, native idiom |
| Share score (NO X/Twitter) | ✅ Web Share | ✅ ShareLink | ✅ QR | ✅ Intent | Decision 022; web has clipboard fallback |
| Spoiler-free emoji-grid result | ✅ | ✅ | ✅ | ✅ | Wordle-style 🟩🟥; the daily share loop (ROADMAP #1). tvOS renders it on `TVResultsView`. **Audit 2026-06-30: Android shipped it** — `ResultsScreen` renders 🟩🟥⬛ and shares it (was falsely ⏳) |
| First-run onboarding | ✅ | ✅ | ✅ | ✅ | 3-card play/learn/compete walkthrough — the order is deliberate and identical everywhere: play, then LEARN, then compete, because learning is the point rather than a side effect. **Android shipped 2026-06-30** (`OnboardingScreen`, emulator-verified). **web + tvOS shipped 2026-08-03**, each in its own idiom: the web uses a native `<dialog showModal>` (focus trap + ESC for free — no hand-rolled overlay) with the three cards paged by a Next button, and never shows it in front of a shared `#/quiz/` or `#/item/` link, because someone who followed a link was sent somewhere specific and a walkthrough in the way is a toll booth. tvOS shows all three beats on ONE screen with exactly one focusable control — a paged carousel is a chore on a remote, and a living-room audience is often several people reading one screen. tvOS shares macOS's `tidbits.hasOnboarded` key so a household doesn't see it twice. Verified in Chrome (all three cards stepped through, flag persisted) and on the Apple TV 4K simulator. |
| Leaderboards | 🔮 Supabase | 🚧 Game Center | 🚧 Game Center | 🔮 Play Games | Apple **code complete**: auth (now presents the sign-in sheet) + score submission on game-end (classic high + daily streak) + the dashboard (Settings → "Leaderboards & Achievements") + access point. No-op until authenticated; the only thing left is **creating the leaderboards in App Store Connect** with the matching IDs (`docs/GAME-CENTER-SETUP.md`) |
| Achievements | 🔮 | 🚧 Game Center | 🚧 Game Center | 🔮 Play Games | Apple **code complete**: **9** achievements reported from the shared `RecordsStore` (first game / flawless / centurion / 7- & 30-day streak / full pie / Stake sharpshooter / explorer / scholar), partial-progress where it makes sense. Pending **ASC achievement creation** with matching IDs (`docs/GAME-CENTER-SETUP.md`) |
| Challenges (friend score/achievement) | 🔮 | 🚧 Game Center | 🚧 Game Center | 🔮 | iOS-26 async friend challenges — **code complete** (challenge listener registered; "Play" launches Classic). Rides the leaderboards/achievements; enable "challengeable" per board in ASC. Fits async-first (Decision 023), no backend. **Activities deliberately skipped** (would fork the Bonjour/Supabase multiplayer story; Apple-only) |
| Local pass-and-play | 🔮 | ✅ | ⏳ | ✅ | 2–4 players, shared fair question set, hand-off + scoreboard. **Android shipped 2026-06-30** (`PartyContainer`, emulator-verified: setup → handoff → turns → ranked scoreboard + share). **Windows ✅** (WINDOWS-PARITY 1.4). **macOS ✅ 2026-07-31** — the Mac was the one desktop without it; `macOS/MacParty_macOS.swift` reuses Core verbatim and rebuilds only the shell (window-root swap like Versus, `CompactButtonStyle` + keyboard shortcuts, grape Home card). Verified end-to-end on the real app. **Tie bug fixed on every platform the same day** — a level board announced "Player 1 wins!"; now one shared rule (`StandingsOutcome`) says "It's a tie!" / "Tie — A & B" and highlights every leader. tvOS ⏳ (lean-back), web 🔮 |
| Spaced re-asking of missed facts | ✅ | ✅ | ✅ | ✅ | Due misses woven into corpus-MCQ games (skips Daily + non-MCQ modes); resolve on correct. **Opt-out toggle** ("Review questions") on every platform: iOS/tvOS via `GameSettings.reviewKey` @AppStorage (both now in their Settings screen), web + Android via a Records→Settings switch. Default ON |
| Haptic feedback | n/a | ✅ | n/a | ✅ | Correct/wrong; Settings toggle. **Android shipped 2026-06-30** (`GameHaptics` via `View.performHapticFeedback` CONFIRM/REJECT, honors the Settings toggle) |
| Settings (haptics, reset, attribution) | ◑ | ✅ | ✅ | ✅ | iOS + tvOS: full Settings (review toggle / reset seen / reset all records / Game Center status / attribution; haptics is n/a on tvOS). tvOS reached from the home header. **Android shipped full Settings 2026-06-30** (`SettingsScreen` via Home gear: haptics + review toggles, Material You toggle, reset seen, reset all records, version, Wikipedia CC BY-SA attribution — emulator-verified). web: "Review questions" toggle so far (Records→Settings); reset/about still pending there |
| Async head-to-head / groups | 🔮 | ⏳ Game Center | ⏳ | 🔮 | Async > real-time for survivability (ROADMAP) |
| Tidbits Live — join a Mac-hosted event (any platform) | ✅ join | ✅ join | ✅ join | ✅ join | **Cross-platform join over Firebase RTDB `live/{code}` (Decision 043; `docs/LIVE-ROOM-CONTRACT.md`).** A Mac host publishes `meta`/`pub`/`scores`; every player owns `teams/{uid}` + `answers/{qid}/{uid}` and streams the host's question state. **One backend all reach** (GameKit is Apple-only, mDNS can't reach a browser). Surfaces: **web** `js/live.js` (`#/live/CODE`); **iOS/iPadOS + tvOS** the shared Core `LivePlayerClient` (Swift RTDB REST twin, no SDK) behind `LiveJoinView` / `TVLivePlayerView`; **Android** `FirebaseNet.live*` + `LiveRoomScreen` (real Firebase SDK). **Unified "Join a game"** on iOS/tvOS/Android: one code box confirms a `live/{code}` room exists and joins it — a Tidbits Live event OR a Trivia Night, same backend (Decision 044); a miss is a clear "no game found." Same verb, the player needn't know which product hosts them. Build-verified (iOS/tvOS/macOS Xcode-beta, Android Gradle) + iOS join form render-verified; **the live multi-device run is the owner gate** (rules deployed). Tidbits Live's HOST is Mac-exclusive (the marquee); a casual Night can be hosted from any platform (row below) |
| Trivia Night — networked (host from ANY platform, join from any) | ✅ host+join | ✅ host+join | ✅ host+join | ✅ host+join | **Unified on the shared RTDB backend (Decision 044, amends 033) — host from iOS/iPadOS/tvOS/Android AND web; join from all + the Mac.** A casual Trivia Night is now a `live/{code}` room, the SAME backend as the Mac-hosted Tidbits Live — so the browser can finally host/join (no mDNS). The host (`LiveHostNet` + Core `LiveNightHost` on Apple; `FirebaseNet` host methods + `NightHostScreen` on Android; `js/firebase.js` + `openNightHost` on web) opens a room, shows a **join code + scannable QR** (`tidbitstrivia.com/live/CODE`), publishes each question (answer withheld until reveal), **auto-scores on reveal**, and paces **Reveal → Next** with live standings. **"I'll play too" toggle** — the host either runs it (game master) or answers on their own device and is scored. Players join via the unified "Join a game" (`LivePlayerClient` / `FirebaseNet` / `js/live.js`). **Verified:** iOS host opened a real room (code `45ZB`) against live Firebase; iOS/tvOS/macOS build green (Xcode-beta), Android `assembleDebug`, web `node --check`. **The multi-device run is the owner gate** (rules deployed; needs a real host + a phone). The **old mDNS/Bonjour stack** (`NightHost`/`NightClient`/`BonjourTransport`/Wi-Fi Aware/BLE, `net/Night*`) is **legacy** — no longer wired; `LiveNight` survives only for GameKit Quick Match (Decision 039). **ALL question types are playable in a hosted night (2026-07-03):** the `pub`/`answer` contract carries every type's data (image, numeric bounds, shuffled order/match items, enum target) and each host auto-scores on reveal from its OWN local question (exact / proximity / partial-credit / alias-match) — nothing that could leak an answer is published. Joiner answer surfaces on all 4 platforms: picture (image + options), numeric (slider/stepper), ordering (reorder), matching (pickers), type-answer (text), enumerate (chips). tvOS falls back to "answer on a phone" for reorder/match/enumerate (Siri Remote). ROADMAP #4 |
| Tidbits Live — Wave A run-of-show depth (2026-07-04) | ✅ join | ✅ join | ✅ join | ✅ join | **Wave A COMPLETE (host authoring & run-of-show depth), 1.6.27.** macOS HOST (builder + cockpit): "story behind the answer" on reveal, difficulty/category **balance meter**, per-round **timers**, **drag-to-reorder**, **CSV import**, per-round **host notes**, and a **final wager round** (mark a round Wager → teams stake 0…their score → correct +stake / wrong −stake on reveal, clamped to score). JOIN-CLIENT display on **ALL FOUR** (web/iOS/tvOS/Android): story on the reveal screen, ticking countdown (coral ≤5s), wager stake input (iOS Stepper / web+Android slider / **tvOS focusable −/+ buttons**). Big screen (macOS) shows story + countdown. Wire: `Pub.story/deadline/wager` + `Answer.wager` + `LiveRound.{timerSeconds,hostNote,isWager}` (all optional/additive). All 5 build green |
| Tidbits Live — Wave B AV & show production (2026-07-04) | 🚫 | 🚫 | 🚫 | 🚫 | **macOS HOST-ONLY (the emcee cockpit + big screen); shipped 1.6.29.** All serverless: **SFX/stinger board** (5 synthesized sounds on number-key shortcuts, nothing licensed), **selectable audio output** (route show audio to the venue PA via CoreAudio), **audio clip playback** + **structured audio rounds** (BYO-clip folder → name-that-track typeAnswer questions, played to the PA), **speed-tiered scoring** (fastest correct +3/+2/+1, any round), **looping music beds** (background ambiance, level slider, layers under clips/stingers), **video questions** (clips on the big screen via AVKit), and **deeper big-screen chrome** (format + difficulty labels). Join clients answer the audio/video rounds as normal typeAnswer questions (they hear/watch in the room) — no client change. Fixed a latent sandbox bug (missing `user-selected` file entitlement broke NSOpenPanel reads → CSV import + clips). Needs a signed build to exercise (audio/file access). Host is Mac-exclusive by design (the marquee), so 🚫 for the four consumer platforms. **2026-09-01: WINDOWS now has authored audio/video rounds too** — `LiveEvent.RoundClips` (plain paths, not security-scoped bookmarks; deliberately outside the portable event file per LIVE-EVENT-FILE §3.1) plus a cockpit Play/Stop row and a real "clip unavailable" state. See `docs/WINDOWS-PARITY.md` 3.32 |
| Tidbits Live — Wave C submission & scoring (2026-07-05) | ✅ signal | ✅ signal | 🚫 | ✅ signal | **Mostly macOS HOST; the focus signal is cross-client. Shipped 1.6.30.** All serverless: **hybrid paper+digital big-screen leaderboard** (networked phone teams + in-room paper teams in ONE ranked standings — the differentiator), **CSV export** of unified standings, **name moderation gate** (hide an offensive networked name from the big screen), **answer-lock timer** (auto pencils-down at the round timer + manual lock — first real use of pub.locked), **team merge** (reconcile a split paper team), **brains-only tie-break** (phones-down manual pick, alongside the existing numeric closest-wins; final-wager = a Wave A wager round). The **tab-switch/focus cheating signal** IS cross-client: web/iOS/Android join clients flag a player leaving the app mid-question (`Answer.blurred`) → host shows a coral flag (the ✅ join cells; tvOS 🚫 — a TV doesn't tab-switch). macOS host build + Android build + web all shipped |
| Cross-venue / season leaderboard + defendable titles (Wave E, 2026-07-05) | ✅ | ✅ | ✅ | ✅ | **The moat, shipped 1.6.31 — the $0 two-plane §M architecture.** WRITE: when a live night ends, each join client writes its cumulative per-venue/season standing to `standings/{season}/{venueKey}/{authUid}` (keyed by the AUTH uid the account-linking fix stabilised; byte-identical `currentSeason()`/`venueKey()` in Swift/Kotlin/JS). AGGREGATE: an **hourly GitHub Actions cron** (`tools/aggregate_leaderboard.py`) reads standings via RTDB REST (now public-read), ranks per venue + cross-venue, commits `data/leaderboard/*.json` → Pages. READ: all four clients fetch the **static JSON** (free/cacheable, never RTDB) and render the season overall + per-venue standings — web `#/leaderboard`, iOS/tvOS `LeaderboardView`(+`_tvOS`) via shared Core `LeaderboardAPI`, Android `LeaderboardScreen`. **Defendable titles:** each board marks the CHAMPION (#1) + highlights the signed-in player's own row (matched on the auth uid). Degrades to stale-not-billed at any ceiling; no card, ever. Populates once real live nights end |  **macOS GAP (found 2026-09-01):** the Mac has NO leaderboard surface at all — its sidebar is Play/Records/Create/Tidbits Live and `LeaderboardView` exists only for iOS and tvOS. The Mac harness was asserting a screen that does not exist and silently grading Play instead.
| Daily — global board layer (2026-07-19) | ✅ | ✅ | 🚧 | 🚧 | **The Daily's global-ranking layer (MONETIZATION §4c, `docs/DAILY-BOARD-CONTRACT.md`). NOT a separate mode** — owner flagged that a first-draft "Daily Six" competed with the Daily Tidbit for placement/purpose, so the board FOLDED INTO the one Daily everyone already plays (same 7 questions, same "Daily Tidbit" name, streak + archive unchanged). On completing today's Daily, the client writes `dailyBoard/{day}/{uid}` and offers "See how the world did": rank + percentile (from a published score histogram, computed locally) + per-question global accuracy + top board. The hourly cron (`tools/aggregate_dailyboard.py` + `dailyboard.yml`) ranks + publishes `data/dailyboard/{day}.json`; clients read the static JSON, never RTDB (R-NET-1). Byte-identical `pickDaily(...,7)` incl. a Python cron picker proven in `tools/daily-parity/run.sh` (4 stacks). **Playing + streak + world rank all FREE (R-MON-4); Club buys the autopsy.** **Web ✅ shipped + RTDB rules DEPLOYED + live-verified** (write/validate/read enforced on the real DB; end-to-end render checked). **SHIPPED 1.6.46:** viewing UI on **web + iOS + macOS** (shared `DailyBoardContent`); **tvOS + Android + Windows CONTRIBUTE** (submit → globally ranked; Core logic tested — Windows 259 tests green, Android/tvOS build-verified), board-VIEWING UI a uniform fast-follow (tvOS needs a TVTheme dark restyle; Android/Windows need the native board screen). All six platforms are now RANKED in the same global board. Windows column: `docs/WINDOWS-PARITY.md` |
| Push reminders — "the appointment" (2026-07-19) | ✅ | ✅ | 🚫 | ✅ | **The return-trigger for async global modes (docs/PUSH-CONTRACT.md). Verified $0 on all three** (no card, no always-on server) via THREE legs — iOS goes APNs-direct (no Firebase SDK), Android FCM HTTP v1, web Web-Push/VAPID — all from one GitHub Actions cron. **RTDB `pushTokens/{uid}/{platform}` rule DEPLOYED + live-verified owner-only** (mirrors playersPrivate). Cron sender `tools/send_reminders.py` + `reminders.yml` written + safe-no-op-verified (skips cleanly until owner secrets exist). **iOS token capture BUILD-VERIFIED** (`PushManager` app-delegate + prompt-after-Daily + `savePushToken`); the Push CAPABILITY/entitlement + APNs `.p8` are OWNER steps (§Owner setup). tvOS 🚫 (lean-back). **2026-08-03: the two remaining client legs shipped** — Android `notifications/PushTokens` (runtime POST_NOTIFICATIONS ask after a Daily, `FirebaseMessaging.getToken()`, plus `AppFirebaseMessagingService.onNewToken` for rotation; the manifest service had been sitting commented out as "goes here when push ships") and web `js/push.js` + `sw.js` push/notificationclick handlers, storing the whole PushSubscription blob. **And the in-app opt-out, which §Client-behavior required and NO platform had built** — now in iOS Settings → Notifications, Android Settings → Notifications and web Records → Settings; it deletes the token node (a local flag would leave the cron still sending) and the launch-time re-upload honours it. Everything is inert until the owner adds the APNs `.p8` / FCM service account / VAPID keypair — with the placeholder key the web toggle does not even render. |
| Tidbits Live — Wave D venue business capabilities (2026-07-05) | 🚫 | 🚫 | 🚫 | 🚫 | **macOS HOST-ONLY; shipped 1.6.32. Capabilities built, PAYWALL DEFERRED (owner direction).** All serverless: **recurring-series scheduling** (`LiveEvent.weekday` → the builder + saved-events list show "Every [day] · next [date]"; a weekly host reuses the template), **sponsor kit** (`LiveEvent.sponsor` → a persistent "Brought to you by …" big-screen footer through lobby/between-rounds/play), **lead capture** (`LiveEvent.leadCaptureURL` → a "Join [venue]'s mailing list" QR on the final standings; venue keeps its own CRM), **white-label** (`LiveEvent.brandHex` → a ColorPicker recolors the big-screen event title). No pricing/paywall wired. Host is Mac-exclusive, so 🚫 for the four consumer platforms |
| "How did you know that?" (consumer L5, 2026-07-04) | ✅ | ✅ | ✅ | 🚫 | Shipped 1.6.33. The solo results screen adds a "Tough ones you nailed" section — hard questions (difficulty ≥4) answered correctly, each with a "How did you know that? · Share" that shares the win as a conversation opener ("I knew X — how did YOU know that?"). The charter payload (reflection + connection over passive move-on; Water Cooler Trivia's move). tvOS 🚫 (lean-back — sharing from a TV is an edge case) |
| Levelable badges (consumer L4, 2026-07-04) | ✅ | ✅ | ✅ | ✅ | Shipped 1.6.33. A "Badges" section in Records: 5 tiered milestones from existing stats — Scholar (games), On a Roll (longest day streak), Domain Master (domains mastered), Sharpshooter (lifetime accuracy, min 5 games), Regular (live nights). Shared Core `BadgeMath`/`LevelableBadge` (iOS/tvOS) + Kotlin + JS mirrors, identical tiers; tier number as the icon (no emoji chrome), progress bar, hidden until the first is earned. **2026-08-03 — audit correction + the two missing surfaces built.** This row's tvOS cell read ✅ while its own note said tvOS ⏳, and Android read ⏳ while `BadgeMath` + `BadgeRow` had been in `AppRoot.kt` since it shipped: the cells were simply wrong in both directions. The real gaps were **tvOS and macOS**, and both now have the section — same shared `BadgeMath` tiers, each in its own idiom (`TVRecordsCard` rows at the ten-foot ramp; a `chunkyCard` row on the Mac). Screenshot-verified on the Apple TV 4K simulator with 24 seeded games: Scholar 1/4, On a Roll 2/4, Domain Master 4/4 maxed, Sharpshooter 2/4. All six platforms now carry it (Windows via `RecordsViewModel.Badges`). |
| Avatar customization (consumer L4 cosmetics, 2026-07-04) | ✅ | ✅ | ✅ | ✅ | Shipped 1.6.33. Tap your profile avatar to re-roll its color (`rerollAvatar()` on each identity store — re-rolls avatarSeed, persists + syncs like rename). Self-expression / agency, monetization-safe, no pay-to-win. **2026-08-03 — same audit correction:** the cells said tvOS ✅ / Android ⏳ and the truth was the reverse (Android's `Profile.kt` has had tap-the-avatar since it shipped). **tvOS and macOS now have it, in their own idioms** — the TV gets a focusable `TVChipStyle` button ('Shuffle avatar color'), because a remote cannot tap a circle and `.plain` would destroy focusability; the Mac gets a labelled 'Shuffle Color' button beside Rename, because an unlabelled click target with no hover cue is invisible on a pointer platform. Screenshot-verified on the Apple TV 4K simulator. |
| Social graph — "add the people you played with" (L5, 2026-07-04) | ✅ | ✅ | 🚫 | ✅ | **Shipped 1.6.34; the churn-halving strategic payload, owner-chosen.** At live-night end, each join client reads the room roster once (`live/{code}/teams`, already readable) and captures co-players (web `captureCoplayers`, iOS `LivePlayerClient.captureCoplayers`, Android `FirebaseNet.liveTeams`); the wrap screen shows "Add the people you played with" → one-tap add to a **private** friend list under the owner-only `playersPrivate/{uid}/friends` bucket (**no rule change**). Friend store: web `Identity`, Core `PlayerIdentityStore`, Kotlin `PlayerIdentity`. tvOS 🚫 (no join wrap surface / lean-back) |
| Social graph — friends leaderboard filter (L5, 2026-07-04) | ✅ | ✅ | ✅ | ✅ | Shipped 1.6.34, all four platforms. The leaderboard leads with a "Friends" section — your added people ranked by their standing from the already-public overall board (you highlighted, "—" for those not yet on the board). Friends stay private; only their public standings are read. tvOS reuses the Core friend store (`LeaderboardView_tvOS`) |
| Async friend duels (L5, 2026-07-05) | ✅ | ✅ | 🚫 | ✅ | **Shipped 1.6.35; the last big consumer L5 item, owner-chosen.** Challenge a friend to the SAME question set (a "Duel" button on the friends board → a 6-question set frozen at creation); both answer on their own time via the real game engine; higher score wins. Rules (deployed): `duels/{id}` create-once with per-player `players/{uid}` score slots (auth.uid===$uid, so each writes only their own), `duelInbox/{uid}` owner-read (the challenger drops an invite id in the friend's private inbox). Store: web `js/duels.js`, Core `DuelStore`, Kotlin `Duels`. UI: web `#/duels`, iOS `DuelsView`, Android `DuelsScreen` (inbox + your duels with status: your-turn / waiting / Won-Lost-Tied). Serverless, $0. tvOS 🚫 (no game-input surface / lean-back). NOTE: Android duel-id list persists via SharedPreferences (Application-inited); one-way (no rematch-chain yet); rules allow any authed user to write an inbox entry (v1 spam surface, low at scale) |
| Cross-platform online | 🔮 | 🔮 | 🔮 | 🔮 | Supabase, after Apple online proves out (Decision 020) |

---

## 4. Authentication + profile

| Feature | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
> **Audit 2026-06-30: this entire section was fictional** (no SiwA, no
> Credential Manager, no account or deletion path anywhere; the only
> "identity" was Game Center). **Superseded 2026-07-30** — the portable
> identity spine shipped, and with it real Sign in with Apple and real
> account deletion. Cells below are the CURRENT state.

| Sign in with Apple | ✅ | ✅ | ✅ | 🚫 | Shipped. **iOS/macOS/tvOS surface it in Settings** — iOS put it only inside Profile until 2026-07-30, so a phone showed a Game Center section and no way to sign in (owner-reported). Android uses Google instead |
| Sign in with Google | 🔮 | 🚫 | 🚫 | 🔮 | Not built. Apple platforms stay SiwA-only |
| Email/password | 🚫 | 🚫 | 🚫 | 🚫 | Deliberately not offered — federated sign-in only (password entry on a TV remote is hostile, and there is no password to leak) |
| Biometric gate for sensitive actions | n/a | 🔮 | n/a | 🔮 | Still unbuilt; nothing gated on it |
| Account deletion | ✅ | ✅ | ✅ | ✅ | App Store 5.1.1(v). In-app, deletes the real (even anonymous) account. iOS also exposes it in **Settings → Delete account** as of 2026-07-30, not only inside Profile (Decision 048) |

Sign-in is **optional and would gate only sync** — every browse/use verb
works signed-out on every platform (see `per-ecosystem-sync-islands`).
None of it is built yet on any platform.

---

## 5. Universal Links / App Links / deep linking

| Feature | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
| Universal Links / App Links (HTTPS) | n/a | ⏳ | n/a | 🚧 | `/.well-known/` files; tvOS has no Safari hand-off — custom scheme only. **UNBLOCKED + VERIFIED 2026-08-04, by the verifiers themselves.** Android: the Play App Signing SHA-256 was the missing half (assetlinks carried only the UPLOAD key, so links verified only for locally-built installs); both fingerprints are now live and Google's Digital Asset Links API returns 2 statements. Apple: `apple-app-site-association` published + `com.apple.developer.associated-domains` added, and Apple's CDN has ingested and is serving it (`app-site-association.cdn-apple.com/a/v1/tidbitstrivia.com`). NOTE `https://www.` still lacks a certificate — apex is canonical everywhere, `www` is cosmetic (docs/DNS-AND-DEEP-LINK-STATE.md) |
| Custom scheme | n/a | ✅ | ✅ | ✅ | `tidbits://` — **Android shipped 2026-06-30** (manifest BROWSABLE filter + `MainActivity.routeFor` → AppRoot inbox; emulator-verified `tidbits://party` + `tidbits://settings`). iOS handler exists in `.onOpenURL`. **tvOS ✅ 2026-08-03 — and the gap was not what this row said it was.** The scheme was registered on the TV from day one (one Info.plist serves the whole universal target, and the simulator's "Open in Tidbits?" prompt proves the OS resolves it), and `.onOpenURL` posted every link into the store inbox — but **`ContentView_tvOS` never drained that inbox**, so a link launched the app and then did literally nothing. iOS and macOS have always drained it; the TV never did. Now it does, resolving to the tvOS idiom (a cover, since there is no tab bar or nav stack): `daily` starts the Daily, `quiz/<id>` opens Create with the pending id, `item/<id>` opens `SharedItemView_tvOS`. Verified on the Apple TV 4K simulator. |
| URL params reflect filter state | ✅ | n/a | n/a | n/a | Web-specific affordance |
| Canonical share URLs (`https://…/item/{id}`) | ✅ | ✅ | ✅ renders | ✅ | **Audit 2026-06-30:** iOS `ResultsView` ShareLink + tvOS still text-only (no URL). **2026-08-03 — the `/item/{id}` twin DEEP_LINKS.md reserved a month ago now exists end to end.** The per-question "how did YOU know that?" share carries it on web, iOS, Android and Windows (one builder per stack: `itemURL`/`QuizSharing.itemURL`/`itemUrl`/`ShareText.ItemUrl`), and every target renders it: **web** `#/item/{id}` (the landing half — a stranger with no app and no account sees the fact, the explanation and the Wikipedia door; the existing `404.html` forwarder makes the clean path work), **iOS** a `SharedItemView` sheet, **macOS** a sized sheet, **Android** a `Route.SharedItem` screen. `tidbits://item/{id}` routes through the same inbox as every other deep link; the https twin needed no new Android intent-filter because the App Link filter is host-wide, so nothing emitted goes undeclared. **Verified on real surfaces:** Chrome (corpus + shape ids and the retired-id path), the iPhone 17 Pro simulator, and a Pixel 9 emulator via `am start`. **tvOS RENDERS but does not emit** — a TV has no share sheet, but it can be sent a link (Top Shelf, Siri, a QR on the big screen), so `SharedItemView_tvOS` shows the same fact at the ten-foot ramp. It deliberately omits the "Read on Wikipedia" link the other four carry: there is no browser on the TV, so that would be a dead end rather than a door. |

---

## 6. Notifications

| Feature | Web | iOS | tvOS | Android | Notes |
|---|---|---|---|---|---|
| Push notifications | 🚫 | 🔮 APNs | 🚫 | 🔮 FCM | Web push too inconsistent; TV notifications are hostile in a living room |
| Cross-platform dispatcher | n/a | 🔮 | n/a | 🔮 | One Worker, two transports (APNs + FCM) — symmetric payload |
| Notification permission request | n/a | 🔮 | n/a | 🔮 | At opt-in moment, NOT app launch |

---

## 7. Payments / subscription

| Feature | Web | iOS | tvOS | Android | Notes (macOS + Windows folded in) |
|---|---|---|---|---|---|
| Tidbits Club — entitlement spine (`isClub` gate, fail-open, cached last-known-good) | ✅ | ✅ | ✅ | ✅ | **The one gate every Club feature checks (MONETIZATION §7, Decision 047). SHIPPED all 6 platforms:** web `js/entitlement.js`, Apple `Core/Networking/EntitlementStore.swift` (macOS ✅ + tvOS ✅ env-injected), Android `data/Entitlement.kt`, **Windows `Tidbits.Core/Networking/EntitlementStore.cs` (285 tests green)**. `isEntitled = localStore(Class A) OR remote(Class B)`; a transient miss NEVER revokes (fail open). RTDB `entitlements/{sha256(email)}` rule DEPLOYED + live-verified (clients read-only, R-MON-3). |
| Tidbits Club — paywall UI (native, R-MON-2: sign-in only, no code field) | ✅ | ✅ | ✅ | ✅ | **COMPLETE all 6 platforms.** Web `#/club` ✅, iOS `ClubPaywallView` ✅, macOS `.sheet` ✅ + tvOS `.fullScreenCover` ✅ (both BUILD SUCCEEDED), Android `ui/ClubPaywall.kt` full-screen route ✅ (assembleDebug green), **Windows `ClubPaywallView` in an FAContentDialog ✅ (288 tests green, headless-PNG verified; windows-latest CI green)**. Content = 1 pitch + 4 pillars + 3 plans (see `docs/CLUB-MARKETING.md` §2/§4). |
| Tidbits Club — native purchase (Class A store) | ✅ MoR | 🚧 | 🚧 | 🚧 | Apple StoreKit 2 code ✅ (`StoreKitStore.swift`, Universal Purchase — iOS/macOS/tvOS), **Windows Microsoft Store IAP ✅ SHIPPED 1.6.75** — `WindowsStoreGateway` now compiles into the MSIX and returns the three certified add-ons (the owner's tax/payout profile cleared them 2026-08-07). It lives in a separate `Tidbits.Windows` **`net10.0-windows` class library**, loaded by reflection, because that TFM on `Tidbits.App` breaks every publish with `MSB4062 ExpandPriContent` (Decision 056; WINDOWS-STORE-SUBMISSION §7). Product ids verified against the Store's own API (`windows-store-addons.yml`). **A real purchase is verifiable ONLY by installing the certified MSIX from the Store** — `StoreContext` returns nothing without a package identity, so CI cannot exercise it, **Android Play Billing 🚧 in progress**, web MoR webhook ✅ (`workers/tidbits-auth`). **Store products were created 2026-07-23 — this gate is CLEARED.** Remaining 2026-08-04: Apple's two SUBSCRIPTIONS (`club.monthly`, `club.annual`) are `READY_TO_SUBMIT` and were never submitted, while `club.lifetime` is `WAITING_FOR_REVIEW` — so the 3-plan paywall would launch with only 1 purchasable plan. Lemon Squeezy is still in TEST mode pending their store review, so no real web charge can settle yet. |
| Cross-platform entitlement sync (buy once, member everywhere) | ✅ | ✅ | ✅ | ✅ | Any platform's purchase → the shared `entitlements/{key}` plane (web) or the local store attestation (native); every client reads it. $0 (Firebase Spark + Cloudflare Worker). |
| **Delete Account (in-app, App Store 5.1.1(v))** | ✅ | ✅ | ✅ | ✅ | **2026-07-29, from an App Review 5.1.1(v) rejection (tvOS 1.6.52/96).** `PlayerIdentityStore.deleteAccount()` deletes every account-keyed RTDB node (`players/{key}`, `dailyLog/{key}`, `emailOwners/{key}`, `playersPrivate/{authUid}`, `pushTokens/{authUid}`, known `dailyBoard`/`standings` rows, and only MY slot of a shared duel), then the Identity Toolkit user itself (`accounts:delete`), then wipes local SwiftData + UserDefaults + Keychain and re-bootstraps a fresh anonymous identity. Surfaced in the **Profile/Account** section on **iOS ✅ (`ProfileView`), macOS ✅ (Settings → Account), tvOS ✅ (Settings → Profile, screenshot-verified)** — shown whether or not the player signed in with Apple, because Tidbits provisions a real anonymous account for everyone. Needed a rule change: `emailOwners/$key` now permits an owner DELETE (a delete sends `newData == null`, which the old write rule rejected). **Web ✅ + Android ✅ 2026-08-03** — the mirror shipped, so all six platforms now delete for real. Web: `Identity.deleteAccount()` + `FirebaseNet.removePath`/`deleteAuthUser`, surfaced on the Profile view under the sign-in block (shown signed in or out). Android: `PlayerIdentity.deleteAccount(store)` + the same two net primitives, surfaced BOTH in Settings → Account and on Profile beside Sign out, sharing one `DeleteAccountConfirm` dialog so the wording and the failure path cannot drift. Both wipe the local side too (`Store.clearAccountData()` on Android is deliberately wider than `resetAllRecords()` — a deleted account must not leave its Daily history for the next person on the phone). **Verified end-to-end on the real backend** (Chrome against live Firebase): credential deleted, a fresh anon uid minted, local daily results cleared, new profile written, no error. |
| **One Club door (R-CLUB-1) — every Club feature inside a single hub** | ✅ | ✅ | ✅ | ✅ | **2026-07-29, owner directive:** *"the whole point of Tidbits Trivia is that we are the world's best trivia app with the least amount behind a paywall"*. Club used to surface as SEVEN separate locks — four cards on Home (Link Wall, Weak-Spot Arena, Marathon, Expeditions), three "see all" rows in Records (Story Archive, Knowledge Atlas, Marathon History) — plus a Club row in Settings, which read as a freemium funnel. Now: **one quiet `Tidbits Club` row on Home**, members → a new `ClubHubView` listing all six features with their live state, non-members → the existing `ClubPaywallView` (owner's call: keep the paywall as the non-member view). **Records is free-tier only** and Settings has no Club row on any platform. Shipped **iOS ✅ (`ClubHubView`), macOS ✅ (`ClubHubView_macOS`, sized sheet), tvOS ✅ (`ClubHubView_tvOS`, sub-surfaces swap content instead of stacking a second cover — Decision 048)**; all three simulator-verified. Rule R-CLUB-1 authored in iOS-DESIGN §5.2a–c and mirrored into tvOS/macOS-DESIGN. **Shipped on ALL SIX** — web (`clubHub()` on the existing `#/club` route, entitlement-aware; seven dead card functions removed), Android (`Route.ClubHub` + `ClubHubScreen`), Windows (`ClubDoorPanel` + an FAContentDialog hub; Records lost its two game hosts, 395 tests green, windows-latest CI green). Verified live per platform: iOS + tvOS simulators, Chrome for web, a Pixel 9 emulator for Android. **Two bugs the verification caught:** the tvOS hub first used `TVAtlasCardStyle` (solid teal on every row — eight shouting rows, grey-on-cyan subtitles) and every platform's hub reused the non-member `previewLine()` copy, which pitches Club at someone who already paid. Both fixed. |

---

## 8. Backend services / shared data plane

All clients consume the same backend / published data. List the
canonical services and assets here so references stay aligned.
If the app has a content data plane, the full contract lives in
`docs/DATA-CONTRACT.md` — this table just indexes it.

| Service / asset | Purpose | Where | Consumed by |
|---|---|---|---|
| <!-- e.g. catalog.sqlite.zz | full content DB, query-on-disk | GitHub Release (rolling) | iOS, tvOS, Android download+inflate; web via index --> | | | |

---

## 9. Web-specific affordances

These are web-only by design; other platforms handle the same
need natively.

| Feature | Web | Why |
|---|---|---|
| URL params reflect filter state | ✅ | Shareable deep links; native apps use in-memory state |
| Web Share API + clipboard fallback | ✅ | iOS/Android use the system share sheet; tvOS shows a QR code |
| View Transitions API (cross-view) | ✅ | iOS uses `.navigationTransition(.zoom)`; Android `sharedBounds` |
| Container queries on components | ✅ | Native platforms use size-class branching |
| Installable PWA + offline shell | ✅ | The zero-install reach play; stores cover the rest |

---

## 10. iOS-specific affordances

| Feature | iOS | Why |
|---|---|---|
| Liquid Glass tab bar / toolbar | ✅ | Web uses `backdrop-filter`; Android M3 tonal elevation |
| Live Activities / Dynamic Island | 🔮 | No equivalent elsewhere — accept the asymmetry |
| WidgetKit home-screen widgets | 🔮 | tvOS analog is Top Shelf; Android analog is Glance widgets |
| Hardware-keyboard shortcuts | ✅ | Web n/a (browser conflicts); Android Ctrl+1..5 on tablets |
| Picture-in-Picture + background audio | 🔮 | tvOS PiP exists but TV apps suspend in background |

---

## 11. tvOS-specific affordances

These are ten-foot / lean-back idioms by design. The general rule:
**idle/ambient surfaces belong to lean-back devices** (TV first,
iPad/tablet/desktop second, phones rarely).

| Feature | tvOS | Why |
|---|---|---|
| Top Shelf extension | ⏳ | The marquee surface when your icon is focused on the TV home screen; reads an App Group snapshot the app refreshes via `BGAppRefreshTask` |
| Siri "Up Next" via NSUserActivity | 🚫 | **Closed 2026-08-03 as not-applicable, with the reason.** "Up Next" is the Apple TV app's VIDEO queue — an app joins it by donating a playback activity for a movie or episode. Tidbits has no video content to queue, so there is nothing to put there; the row was written from a media-app template. The thing it was reaching for — "let Siri and the system offer Tidbits" — is what App Intents does, and that row above is now ✅. A 🚫 with a reason beats a ⏳ nobody can ever close. |
| App Intents voice launches ("surprise me") | ✅ | **2026-08-03.** Two intents, no more: `SurpriseMeIntent` ("Surprise me in Tidbits" / "Play a random round in Tidbits") and `PlayDailyIntent`. An intent that can't be said in one breath won't be, and a Shortcuts list of near-duplicates is worse than a short one. Both `openAppWhenRun` — a trivia round is not a background task. The plumbing is the interesting part: an intent runs BEFORE the scene exists on a cold launch and has no reference to `AppStore`, so it writes to `IntentInbox` (UserDefaults-backed, consumed on read) and the root drains that into the same deep-link inbox every other external entry point uses. Shared by iOS + tvOS; `DeepLink.surprise` is handled on iOS, macOS and tvOS. Building it also fixed a real gap: `tidbits://daily` on iOS only switched TABS and left you to find the card — macOS has started the round from that same link all along. |
| Focus-driven UI (no pointer, no touch) | ✅ | The defining constraint — see `tvos-platform-patterns` |
| Idle screensaver / ambient mode | 🔮 | Lean-back idiom; opt-in, never over playback |
| Layered parallax app icon (imagestack) | ✅ | **Cell was stale — verified 2026-08-03 by inspecting the asset catalog.** `App Icon & Top Shelf Image.brandassets` holds a real parallax stack: opaque Back layers and **transparent** Front layers at 400×240 @1x / 800×480 @2x (1280×768 for the App Store icon), plus Top Shelf images standard (1920×720) and wide (2320×720) at 1x + 2x. Generated by `tools/branding/make_tvos_icon.py`. The Top Shelf EXTENSION (dynamic shelf) is the row above and is still open — that is code, not art. |

---

## 12. Android-specific affordances

| Feature | Android | Why |
|---|---|---|
| Predictive back gesture | ✅ | **Shipped 2026-06-30** — `android:enableOnBackInvokedCallback="true"`; Compose `BackHandler` drives the back stack. iOS swipe-back is fixed-animation; Android is user-driven |
| Adaptive icon (foreground / background / monochrome) | ✅ | **Shipped** (audit 2026-06-30) — `mipmap-anydpi-v26` adaptive icon WITH a monochrome layer (themed-icon ready). iOS uses static; tvOS uses layered imagestack |
| App Shortcuts (long-press app icon) | ✅ | **Shipped 2026-06-30** — static `shortcuts.xml` (Daily / Trivia Night / Pass & Play) firing `tidbits://` deep links. iOS has AppIntents; tvOS has Top Shelf |
| Material You dynamic color (opt-in) | ✅ | **Shipped 2026-06-30** — Settings → "Use system colors" toggle drives `AppTheme(dynamicColor)` (Android 12+). Brand theme default. Other platforms have brand-only theming |
| Google Cast sender | 🔮 | AirPlay analog; needs Cast SDK + device-tested receiver |
| 16 KB page size support | ✅ | Satisfied by AGP 9.2 + `targetSdk = 36` (16 KB-aligned native libs by default). Mandatory for new releases targeting Android 15+ |

---

## Maintenance protocol

When you ship a feature:

1. Find the row in this table. Add new rows under the right section
   if needed.
2. Update each platform's status with one of the legend symbols.
3. Link to the relevant section of the platform's binding design doc.
4. Note any platform-specific deltas in the Notes column.

When a feature ships on one platform but is meaningfully different
elsewhere, add an entry to §9 / §10 / §11 / §12.

When a platform explicitly rejects a feature, add an "Out of scope"
row in the relevant design doc and link from this table.

**Run a parity audit** (the `cross-platform-parity-discipline` skill,
"audit" mode) before any launch wave and roughly once per milestone:
walk the shipped feature list per platform and verify every cell is
honest. Real audits on shipped apps have found both missing rows
(features nobody recorded) AND false cells (a "synced" claim that
never actually synced) — the audit is what keeps this file true.
